GPT Image 2. Edit the FIRST attached image only. It is an orthographic sheet. Keep its entire resolution1536x1024 and its flat middle-grey background. Keep ALL existing labels, scale bars, TOP view, skyline upper edges, spire tips, tower, chimneys, and view positions pixel-identical where possible. Only extend the existing bottom flat fill of FRONT and SIDE DOWNWARD by52.5pixels, from y667.5 to the already-drawn horizontal baseline at y720. Do not extend below y720. The SECOND image is the dimension guide showing that correct bottom edge; use it only to locate the baseline. This closes the blank band between the blue silhouette bottom and its grey baseline. Nothing else changes. Both elevation outlines extend fromx56tox456 andx568tox968; topmost tip y590 and new bottomy720, exactly400px wide130px tall at5px/metre, ground0m top26m. Two eight-panel rings unchanged. Sixteen flats: one broken spire, one four-legged square tank, six chimneys, eight gables, counts unchanged. Do not add, erase, resize, shift or reinterpret any upper silhouette details. No new symbols, flags, lettering, insignia or portraits. Keep background flat grey, NOT white.
