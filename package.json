{
  "name": "secret-dictator",
  "version": "2.0.0",
  "private": true,
  "description": "Secret Dictator v2 — single-player social deduction against AI opponents.",
  "type": "commonjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node test/engine.test.js 50",
    "test:controller": "node test/controller.test.js",
    "parity": "node scripts/driver-parity.js",
    "simulate": "node scripts/simulate.js",
    "verify": "npm test && npm run test:controller && npm run parity && npm run build"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  },
  "dependencies": {
    "three": "^0.185.1",
    "three-mesh-bvh": "^0.9.14"
  }
}
